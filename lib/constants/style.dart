// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

Color light = Color(0xFFF7F8FC);
Color lightGrey = Color(0xFFA4A6B3);
Color dark = Color(0xFF363740);
Color active = Color(0xFF3C19C0);
Color insert = Color(0xFFE3F2FD);

//TODO: need to define a color palette for each of the theme we want to implement
//two ways, either define a theme for myApp, or manually set every widget color
//each off the choice need to planned to avoid issues
Color surface = Color(0xFF3C3C46).withOpacity(1);
Color tabletext = Color(0xFFFFFFFF).withOpacity(1);
Color colorHeaderText = Color(0xFFFFFFFF).withOpacity(0.6);
Color divider = Color(0xFFFFFFFF).withOpacity(0.12);
